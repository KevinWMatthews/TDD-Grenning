#include "IO.h"

void IO_Write(ioAddress addr, ioData data)
{
  //production code
  //will get substituted for
}

ioData IO_Read(ioAddress addr)
{
  //production code
  //will get substituted for
  return -1;
}
