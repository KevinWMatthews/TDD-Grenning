Want to be able to rig this up with templates.

Can currently only nest three subdirectories deep (see clean_path in MakefileWorker.make) until I figure out how to loop it properly.

Check for memory leaks
